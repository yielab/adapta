"""LoRA training: trainer, evaluator, dataset/eval models, provenance.

Import from the concrete modules (adapta.training.trainer, .evaluator, .models,
.provenance) — this package intentionally re-exports nothing.
"""
