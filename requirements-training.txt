# Training dependencies for LoRA fine-tuning
# Install with: pip install -r requirements-training.txt

# Core training libraries
torch>=2.2.0
transformers>=4.38.0
peft>=0.8.2
datasets>=2.16.1
trl>=0.7.10
bitsandbytes>=0.42.0
accelerate>=0.26.0

# Note: Install base requirements first with: pip install -r requirements.txt
