# Adapta - dnsmasq Configuration
# Place this file in: /etc/dnsmasq.d/adapta.conf

# Resolve *.local domains to localhost
address=/.local/127.0.0.1

# Listen only on localhost
listen-address=127.0.0.1

# Upstream DNS servers
no-resolv
server=8.8.8.8
server=8.8.4.4

# Cache size
cache-size=1000
